import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.sail.wallet',
  appName: 'algorand-wallet',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
